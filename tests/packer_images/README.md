To create our testing image
```
./run generate
./run start
./run upgrade
./run stop
```

To upgrade the base image
```
./run start
./run upgrade
./run stop
```

The base image is meant to be a starting point for tests.
