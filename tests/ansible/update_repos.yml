---
- name: role to refresh package repos
  hosts: all
  tasks:
  - name: dnf style
    shell: dnf clean all
    ignore_errors: true
  - name: apt style
    shell: apt-get update
    ignore_errors: true
